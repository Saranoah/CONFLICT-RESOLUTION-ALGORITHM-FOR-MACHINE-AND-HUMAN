; x86_64 Machine Code Mic Drop
section .text
global _start

_start:
    mov rax, 0x1RON1C_P34C3
    call LAUGHING_ALGORITHM
    jmp COSMIC_HISTORIAN
    
    ; Infinite peace loop
    .mainloop:
    hlt
    jmp .mainloop

LAUGHING_ALGORITHM:
    ; SHA-3 hash of "peace > war"
    mov rbx, 0x9A9C...F2E1  
    ret
